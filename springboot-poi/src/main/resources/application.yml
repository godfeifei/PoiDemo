server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test?characterEncoding=utf-8
    username: root
    password: root

logging:
  level:
    com.chen.mapper: debug

mybatis:
  type-aliases-package: chen.com.pojo
  mapper-locations: classpath:mapper/*.xml